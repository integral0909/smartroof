<% title 'Free Roofing Estimate - Instant Satellite Roofing Estimates - Smart Roof' %> 
<% description 'Draw your roof to get an free estimate on your roof! Its easy and takes less than a minute' %>

<body style="background:#f4f4f4;">
<%= render "layouts/header" %>
<div class="bg-primary text-white jumbotron jumbotron-fluid mb-0">
  <div class="container text-center">
    <h1>Roofing Blog</h1>
    <p class="lead">Hire a roofing professional that's right for you.</p>
  </div>
</div>


<div class="container-fluid mt-5 mb-5">
  <div class="row">
    <div class="col-md-8 col-12 blog-content">
      <div class="row">
        <% @posts.each do |post| %>
          <div class="col-md-6">
            <div class="card">
              <%= image_tag(post.cover.url, alt: 'Roofing Estimate Help', class: "img-fluid advice-image") if post.cover? %>
              <div class="text-center pt-3">
                <h3><%= post.title %></h3>
                <h6><%= post.created_at.strftime('%b %d, %Y') %></h6>
              </div>

              <div class="pl-3">
                <%= raw(post.body).truncate(358) %>
              </div>

              <div class="text-center">
                <%= link_to "Read More", post %>
              </div>
            <br>
            <% if signed_in? %>
              <%= link_to "Show", posts_path(posts), class: "btn btn-primary" %>
              <%= link_to "Edit", edit_posts_path(posts), class: "btn btn-default" %>
              <%= link_to "Delete", posts_path(posts), class: "btn btn-danger", data: {:confirm => "Are you sure?"}, method: :delete %>
            <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <div class="col-md-4 col-12">
      <%= image_tag("United-States.png", class: "img-fluid") %>
      <div class="input-group input-group-lg">
        <input type="text" class="form-control" placeholder="Enter your address..." aria-label="Search for..." autofocus="">
        <span class="input-group-btn">
          <button class="btn btn-success" type="button">Go!</button>
        </span>
      </div>
    </div>
  </div>
</div>
